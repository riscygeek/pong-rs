# Pong clone made in Rust
This is a small pong clone written in Rust using the SDL2 library.
I used [this font](https://www.mattlag.com/bitfonts/) for rendering the score.

## Building
```
cargo build
cargo run
```
